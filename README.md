# SocialSync - نظام الأوتوميشن الذكي

تطبيق سطح مكتب احترافي لمراقبة وسائل التواصل الاجتماعي واستخراج البيانات تلقائياً.

## 🚀 المميزات

- ✅ **تطبيق سطح مكتب** باستخدام Electron
- ✅ **نظام اشتراكات** مع تحكم كامل في المستخدمين
- ✅ **لوحة تحكم إدارية** لإدارة المشتركين
- ✅ **استخراج ذكي** للأرقام من جميع المنصات
- ✅ **قاعدة بيانات محلية** SQLite (لا حاجة لخدمات خارجية)
- ✅ **نظام توثيق** JWT آمن

## 📋 المتطلبات

- Node.js (v18 أو أحدث)
- npm أو yarn

## 🔧 التثبيت والتشغيل

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إعداد ملف البيئة
انسخ ملف `.env.example` إلى `.env`:
```bash
cp .env.example .env
```

### 3. تشغيل التطبيق في وضع التطوير
```bash
npm run dev
```

سيتم تشغيل:
- السيرفر على المنفذ `3001`
- واجهة Vite على المنفذ `8080`
- تطبيق Electron

## 👨‍💼 حساب المدير الافتراضي

عند أول تشغيل، يتم إنشاء حساب مدير تلقائياً:
- **البريد الإلكتروني**: `admin@socialsync.pro`
- **كلمة المرور**: `admin123`
بيانات الدخول الافتراضية هي:

البريد الإلكتروني: admin@alkhatib.com
كلمة المرور: admin123

⚠️ **مهم**: قم بتغيير كلمة المرور بعد أول تسجيل دخول!

## 🎯 الوصول للوحة التحكم

بعد تسجيل الدخول كمدير، اذهب إلى:
```
http://localhost:8080/#/admin
```

من لوحة التحكم يمكنك:
- عرض جميع المشتركين
- حظر/تفعيل الحسابات
- تمديد فترة الاشتراك
- مراقبة الإحصائيات

## 📦 بناء التطبيق للإنتاج

```bash
npm run build
```

سيتم إنشاء:
- ملفات التطبيق في `dist/`
- ملفات Electron في `dist-electron/`
- ملف التطبيق القابل للتثبيت

## 🗂️ هيكل المشروع

```
├── electron/           # ملفات Electron الرئيسية
├── server/            # Backend (Express + SQLite)
│   ├── index.ts       # السيرفر الرئيسي
│   ├── database.ts    # إعداد قاعدة البيانات
│   └── scraper.ts     # محرك الاستخراج
├── src/               # Frontend (React)
│   ├── pages/         # الصفحات
│   ├── hooks/         # React Hooks
│   ├── lib/           # المكتبات المساعدة
│   └── components/    # المكونات
└── data.db           # قاعدة البيانات (يتم إنشاؤها تلقائياً)
```

## 🔐 نظام الاشتراكات

### للمستخدمين الجدد:
- فترة تجريبية: **7 أيام**
- يتم التحقق من الاشتراك عند كل عملية استخراج

### للمدير:
- تمديد الاشتراك لأي مدة
- حظر/تفعيل الحسابات
- عرض إحصائيات الاستخدام

## 🛠️ التطوير

### تشغيل السيرفر فقط
```bash
npm run dev:backend
`

### تشغيل Electron فقط
```bash
npm run dev:electron
```

## 📝 ملاحظات مهمة

1. **قاعدة البيانات**: يتم إنشاء ملف `data.db` تلقائياً عند أول تشغيل
2. **الأمان**: تأكد من تغيير `JWT_SECRET` في ملف `.env` للإنتاج
3. **النسخ الاحتياطي**: احتفظ بنسخة من `data.db` بشكل دوري

## 🐛 حل المشاكل

### المشكلة: السيرفر لا يعمل
```bash
# تأكد من أن المنفذ 3001 غير مستخدم
netstat -ano | findstr :3001
```

### المشكلة: قاعدة البيانات لا تعمل
```bash
# احذف الملف وأعد التشغيل
rm data.db
npm run dev
```

## 📞 الدعم الفني

للمساعدة أو الاستفسارات، تواصل مع فريق التطوير.

---

**تم التطوير بواسطة**: SocialSync Team  
**الإصدار**: 1.0.0  
**الترخيص**: Proprietary
